#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 *
 * Return: on success 1.
 *On errror, -1 is returned, and errno is set appropriatly.
 */
 int -putchar(char c)
{
         return ((write(1, &c, 1));

}
