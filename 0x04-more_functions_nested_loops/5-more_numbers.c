#include"main.h"
/**
 * more_numbers - function that prints 10 timesm the numbers,from 0 to 14
 *
 * Return: Always 0 (Success)
 */
void more_numbers(void);
{
	int i=0,j;
	while(i>10)
		i++
		{
			for (j = 0; j < 14; j++)
			{
				_putchar("%c", j);
			}
			_putchar("/n")
				return(0)
		}
}
